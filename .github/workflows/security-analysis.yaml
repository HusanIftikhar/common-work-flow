name: delete file and run security analysis

on:
  push:
    branches: [ main, master]

  pull_request:
     branches: [master]

jobs:
  delte-workflow:
    runs-on : ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        
      - name: Remove file 
        uses: JesseTG/rm@v1.0.3
        with:
          path: .github/workflows/test-workflow.yaml

